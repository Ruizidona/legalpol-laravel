<?php

use Illuminate\Database\Seeder;

class EventosSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        //

    	//$eventos = DB::select('SELECT u.id FROM eventos e, users u WHERE u.id = e.id_usuario AND u.id = 1');
    	//$eventos = 1;
    	//dump($eventos);
    	
    }
}
